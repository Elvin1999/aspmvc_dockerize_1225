@model IEnumerable<string>

@{
    ViewData["Title"] = "Home Page";
}


<h1>All Users' Images</h1>
@if (Model != null)
{
    @foreach (var item in Model)
    {
        <img src="~/images/@item" style="width:150px;height:150px;" />
        <h3>@item</h3>
    }
}